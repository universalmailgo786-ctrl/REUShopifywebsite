<section class="section bg-cream">
  <div class="wrap wrap--narrow">
    <h1 class="h1" style="margin-bottom:var(--space-md)">{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Page content", "settings": [] }
{% endschema %}
